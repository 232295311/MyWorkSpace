/**
 * @param {number[]} a
 * @return {number[]}
 */
var constructArr = function (a) {
  //遍历a数组两次 第一次遍历创建辅助数组b，b[i] = a[0]*a[i]*....a[i-1];
  //第二次遍历是倒序的,为了返回题目所需求的数组res, res = b[i]*
  let res = [];
  let tmp = 1;
  for (let i = 0; i < a.length; i++) {}
};
